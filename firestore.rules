rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }

    function getUserRole() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role;
    }
    
    function isAdmin() {
      return isAuthenticated() && getUserRole() == 'admin';
    }
    
    function isTeacher() {
      return isAuthenticated() && getUserRole() == 'teacher';
    }
    
    function isStudent() {
      return isAuthenticated() && getUserRole() == 'student';
    }

    function hasAccessToClass(classId) {
      let userData = get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
      let classData = get(/databases/$(database)/documents/classes/$(classId)).data;
      return (userData.classes != null && userData.classes.hasAny([classId])) || 
              userData.role == 'admin' || 
              (classData.teacher_id != null && classData.teacher_id == request.auth.uid);
    }

    function isEnrolledInClass(classId) {
      let userData = get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
      let classData = get(/databases/$(database)/documents/classes/$(classId)).data;
      return (userData.classes != null && userData.classes.hasAny([classId])) || 
             (classData.enrolledStudents != null && classData.enrolledStudents.hasAny([request.auth.uid]));
    }

    function isStudentAnswer(studentId) {
      return studentId == request.auth.uid || studentId == request.auth.id;
    }

    // Student Answers - nested structure
    match /student-answers/{classId}/answers/{answerId} {
      allow read: if isAuthenticated() && (
        // Students can read their own answers
        (isStudent() && resource.data.studentId == request.auth.uid) ||
        // Teachers can read all answers (app verifies class access)
        isTeacher() ||
        // Admins can read all answers
        isAdmin()
      );
      
      allow create, update: if isAuthenticated() && (
        // Students can write their own answers
        (isStudent() && request.resource.data.studentId == request.auth.uid) ||
        // Teachers can write for their class (app verifies access)
        isTeacher() ||
        // Admins can write anything
        isAdmin()
      );
    }

    // Student Progress collection
    match /student-progress/{progressId} {
      allow read: if isAuthenticated() && (
        // Students can read their own progress
        (isStudent() && isStudentAnswer(resource.data.studentId)) ||
        // Teachers can read all progress for their class
        (isTeacher() && hasAccessToClass(resource.data.classId)) ||
        // Admins can read all progress
        isAdmin()
      );
      
      allow create, update: if isAuthenticated() && (
        // Students can write their own progress
        (isStudent() && isStudentAnswer(request.resource.data.studentId)) ||
        // Teachers can write for their class
        (isTeacher() && hasAccessToClass(request.resource.data.classId)) ||
        // Admins can write anything
        isAdmin()
      );
    }

    // Student Completions collection
    match /student-completions/{completionId} {
      allow read: if isAuthenticated() && (
        // Students can read their own completions
        (isStudent() && isStudentAnswer(resource.data.studentId)) ||
        // Teachers can read all completions for their class
        (isTeacher() && hasAccessToClass(resource.data.classId)) ||
        // Admins can read all completions
        isAdmin()
      );
      
      allow create, update: if isAuthenticated() && (
        // Students can write their own completions
        (isStudent() && isStudentAnswer(request.resource.data.studentId)) ||
        // Teachers can write for their class
        (isTeacher() && hasAccessToClass(request.resource.data.classId)) ||
        // Admins can write anything
        isAdmin()
      );
    }

    // Users collection
    match /users/{userId} {
      allow read: if isAuthenticated();
      allow write: if isAdmin() || (request.auth.uid == userId);
    }

    // Classes collection
    match /classes/{classId} {
      allow read: if isAuthenticated();
      allow write: if isTeacher() || isAdmin();
    }
      
    // Published Assignments collection
    match /published_assignments/{assignmentId} {
      allow read: if isAuthenticated();
      allow write: if isTeacher() || isAdmin();
    }

    // Published Curricula collection
    match /published_curricula/{curriculumId} {
      allow read: if isAuthenticated();
      allow write: if isTeacher() || isAdmin();
    }

    // Curriculum collection (canonical source)
    // NOTE: The actual Firestore collection name used in the app is "curriculum" (singular),
    // not "curricula". We keep both rules for backward compatibility, but the app primarily
    // talks to "curriculum".
    match /curriculum/{curriculumId} {
      // Any authenticated user can read; app enforces role-based visibility
      allow read: if isAuthenticated();
      // Only teachers and admins can create/update canonical curriculum
      allow write: if isTeacher() || isAdmin();
    }

    // Legacy curricula collection (kept in case any old data exists)
    match /curricula/{classId} {
      allow read: if isAuthenticated();
      allow write: if isTeacher() || isAdmin();
    }

    // Top-level student-answers collection (legacy/aggregated docs)
    // Used by some teacher views that query `collection(db, "student-answers")`
    match /student-answers/{docId} {
      allow read: if isAuthenticated() && (isTeacher() || isAdmin());
      allow write: if isAuthenticated() && (isTeacher() || isAdmin());
    }

    // Attendance collection
    match /attendance/{attendanceId} {
      allow read: if isAuthenticated() && (
        // Students can read their own attendance
        (isStudent() && resource.data.studentId == request.auth.uid) ||
        // Teachers can read attendance for their classes
        isTeacher() ||
        // Admins can read all attendance
        isAdmin()
      );
      
      allow create, update: if isAuthenticated() && (
        // Teachers can create/update attendance for their classes
        isTeacher() ||
        // Admins can create/update any attendance
        isAdmin()
      );
    }

    // Announcements collection
    match /announcements/{announcementId} {
      allow read: if isAuthenticated() && (
        // Students can read announcements for their enrolled classes
        (isStudent() && (
          // Check if student is enrolled in the class
          isEnrolledInClass(resource.data.classId) ||
          // Or if it's an admin announcement (all classes)
          resource.data.authorRole == 'admin'
        )) ||
        // Teachers can read announcements if they are the author OR if it's from another teacher/admin
        // This avoids expensive hasAccessToClass() checks that cause permission errors
        // App-level filtering ensures teachers only query announcements for their classes
        (isTeacher() && (
          resource.data.authorId == request.auth.uid ||
          resource.data.authorRole == 'teacher' ||
          resource.data.authorRole == 'admin'
        )) ||
        // Admins can read all announcements
        isAdmin()
      );
      
      allow create: if isAuthenticated() && (isTeacher() || isAdmin());
      allow update, delete: if isAuthenticated() && (
        // Authors can update/delete their own announcements
        resource.data.authorId == request.auth.uid ||
        // Admins can update/delete any announcement
        isAdmin()
      );
    }

    // Activity Logs collection
    match /activityLogs/{logId} {
      // Allow authenticated users to read activity logs
      allow read: if isAuthenticated();
      // Allow authenticated users to create activity logs (for logging system events)
      allow create: if isAuthenticated();
      // Only admins can update or delete activity logs
      allow update, delete: if isAdmin();
    }
  }
} 